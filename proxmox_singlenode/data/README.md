# Dati locali

ISO, template, esportazioni e appunti temporanei del laboratorio. Il contenuto
è escluso da Git e non viene montato automaticamente nel guest. Trasferire i
file con la UI PVE oppure usando `vagrant ssh-config` e SCP.
