# Claude Code

Leggere e rispettare @AGENTS.md come istruzioni condivise del progetto.
Contesto: @README.md e @docs/architecture.md. Per ogni laboratorio leggere sia
README.md sia STEPS.md e verificare la presenza dei due file autonomi
Vagrantfile e Vagrant.start. Vagrant.start è il percorso basic senza provisioner;
STEPS.md deve poter guidare anche una ricostruzione su VM generiche o bare metal.

I comandi locali sono in `.claude/commands/`. Non avviare provisioning o modifiche
host come conseguenza di una semplice richiesta di revisione o validazione.
